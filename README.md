# Using shadow endpoint

Problem with refreshing data on url change using `goto` function in `shadow endpoint`.

It was bug and fixed. See SvelteKit [discussions](https://github.com/sveltejs/kit/discussions/3755)